{
  "scripts": {
    "start": "docker-compose up -d && npm run logs-follow",
    "stop": "docker-compose down",
    "restart": "npm run stop && npm run start",
    "logs-follow": "docker-compose logs --tail=all --follow"
  }
}
